##GTG

# to study the fractions module

import fractions

frac_comm = fractions.Fraction(4, 6)
frac_norm = fractions.Fraction("1.25")

frac_add = frac_comm + frac_norm
frac_mult = frac_comm * frac_norm
frac_dev = frac_comm / frac_norm
frac_sub = frac_comm - frac_norm

frac_conv = fractions.Fraction(*(4/3).as_integer_ratio())

##TYJC
