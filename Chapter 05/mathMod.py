##GTG

#using the math module

import math

consts = math.pi, math.e

#trig
sin_90 = math.sin(math.pi/2)

#getting sqrt
sqrts = math.sqrt(144), 144 ** 0.5, pow(144, 0.5)
abs_val = abs(-144.0)

flt_num = 1.2345
trunc_val = math.trunc(flt_num) # removes all decimal parts
round_val = round(flt_num), round(flt_num, 2)
##TYJC
